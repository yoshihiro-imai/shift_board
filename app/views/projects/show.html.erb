
<p>
  <strong>名前:</strong>
  <%= @project.nickname %>
</p>
<div class=  "Tasks">
<ul>
  <% @project.tasks.each do |task|%>
  <div class= "task">
    <li ><%= task.date%> </li>:
    <li ><%= task.intime%> </li>時〜
    <li ><%= task.outtime%> </li>時
  </div>
    <% end %>
</ul>
</div>
      


<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
