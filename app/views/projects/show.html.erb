    <div class="title2">  <%= "#シフト表"%></div>

<p>
  <%= "名前：#{@project.nickname}" %>
</p>

<table>
  <tr>
    <th>日にち</th>
    <th>IN-OUT</th>
  </tr>
<% @project.tasks.each do |task|%>
  <tr>
    <td><%="#{task.start_time.day}"%></td>
    <td><%= "#{task.intime}時〜#{task.outtime}時"%></td>
  </tr>  
 <% end %> 

</table>



<%= link_to 'Edit', edit_project_path(@project) %> 
<%= link_to 'Back', projects_path %>
