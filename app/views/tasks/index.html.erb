<!DOCTYPE html>
<html>
<head>
    <title>シフト</title>
</head>
<body>

<%= month_calendar events: @protest do |date, tasks| %>
<%="#{date.day}日"%>
  <% tasks.each do |task| %>
    <div>
    <%=link_to"・#{task.nickname} #{task.intime}時~#{task.outtime}時",task_path(task.start_time)%>

    </div>
  <% end %>

<% end %>

</body>
</html>